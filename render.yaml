services:
  - type: web
    name: pixlp-ai
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn server:app --host 0.0.0.0 --port ${PORT:-10000}
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
